# Local dev only (replace with real secrets in deployment)
GOOGLE_API_KEY=
GEMINI_MODEL=gemini-1.5-flash
GEMINI_EMBED_MODEL=text-embedding-004
EMBEDDINGS_ENABLED=true

# Example Supabase connection placeholders
DB_HOST=localhost
DB_PORT=5432
DB_NAME=postgres
DB_USER=postgres
DB_PASSWORD=postgres